import React, {FC, useState} from 'react';
import LoginForm from "../components/LoginForm";

const Login: FC = () => {

    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Login;