import React, {FC} from 'react';
import Auth from "../components/AuthForm/Auth";

const Login: FC = () => {

    return (
        <div>
            <Auth/>
        </div>
    );
};

export default Login;